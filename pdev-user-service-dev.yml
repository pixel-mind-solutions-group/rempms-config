spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pdev_user_management
    username: postgres
    password: rempmsdev@123
    driverClassName: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8883/realms/PIXEL_HIRE
